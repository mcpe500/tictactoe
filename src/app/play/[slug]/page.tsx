const PlayGame = ({ params }: { params: { slug: string } }) => {

    // const [game, setGame] = useState<Game | null>(null)
    return (
        <div>

        </div>
    )
}

export default PlayGame